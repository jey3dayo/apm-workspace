# benjitaylor-agentation

- Upstream: `benjitaylor/agentation`
- Managed here as a curated local copy for APM rollout
- Why here:
  - `agentation` is useful locally but upstream frontmatter triggers APM compile warnings
  - local curation lets us add the missing metadata while keeping the skill available
- Current curated skills:
  - `agentation`
