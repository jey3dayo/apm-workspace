# obra-superpowers

- Upstream: `obra/superpowers`
- Managed here as a curated local copy for APM rollout
- Why here:
  - `brainstorming` is useful locally but upstream frontmatter triggers APM compile warnings
  - local curation lets us add the missing metadata without forking the whole upstream set
- Current curated skills:
  - `superpowers:brainstorming`
