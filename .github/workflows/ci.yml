name: CI

on:
  push:
    paths:
      - ".github/workflows/ci.yml"
      - "AGENTS.md"
      - "README.md"
      - "TODO.md"
      - "llms.md"
      - "apm.yml"
      - "apm.lock.yaml"
      - "mise.toml"
      - "docs/**"
      - "manual-skills/**"
      - "scripts/**"
      - "tests/**"
      - "catalog/**"
  pull_request:
    paths:
      - ".github/workflows/ci.yml"
      - "AGENTS.md"
      - "README.md"
      - "TODO.md"
      - "llms.md"
      - "apm.yml"
      - "apm.lock.yaml"
      - "mise.toml"
      - "docs/**"
      - "manual-skills/**"
      - "scripts/**"
      - "tests/**"
      - "catalog/**"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci-check:
    runs-on: ubuntu-latest
    env:
      APM_WORKSPACE_DIR: ${{ github.workspace }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up mise
        uses: jdx/mise-action@v4
        with:
          cache: true

      - name: Install workspace tools
        run: mise install

      - name: Validate local scripts
        shell: bash
        run: |
          bash -n ./scripts/apm-workspace.sh
          pwsh -NoProfile -ExecutionPolicy Bypass -File ./scripts/apm-workspace.ps1 help >/dev/null

      - name: Run workspace checks
        run: |
          mise run format:catalog-metadata:check
          mise run format:markdown:check
          mise run format:toml:check
          mise run format:yaml:check
          mise run validate:catalog
          mise run smoke:catalog
