# RTK (Rust Token Killer)

RTK の詳細運用は `$rtk` skill を使う。

Codex で常時効かせたい最小ルールは `catalog/AGENTS.md` に直接置く。`@rules/tools/rtk.md` の本文展開や `RTK.md` 分割には依存しない。

この rule file は Claude / rules 配布先向けの薄い導線として残す。詳細なコマンド表、filter 方針、Codex 固有の setup は skill 側へ集約する。
