interface:
  display_name: "dotenvx Env Ops"
  short_description: "Operate dotenvx env files and clean CLI environments safely"
  default_prompt: "Use $dotenvx-env-ops to decide whether this command should run with dotenvx, a clean environment, or sanitized AWS variables."
