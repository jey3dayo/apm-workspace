# mise Configuration Template
#
# This template provides a comprehensive starting point for mise configuration
# Customize this file based on your project's technology stack
#
# Documentation: https://mise.jdx.dev
# File locations:
#   - User global: ~/.config/mise/config.toml
#   - Project-local: ./mise.toml

# ========================================
# Settings
# ========================================
[settings]
# Number of parallel jobs for mise operations
# Defaults to number of CPU cores
jobs = 8

# Enable paranoid mode (stricter checks)
paranoid = true

# Verbose output (useful for debugging)
# verbose = true

# Experimental features
# experimental = true

# ========================================
# Environment Variables
# ========================================
[env]
# Project-wide environment variables
# These are set when mise is activated in the project directory

# Development environment
NODE_ENV = "development"

# Rust backtrace for better error messages
RUST_BACKTRACE = "1"

# Custom project paths
# PROJECT_ROOT = "{{ cwd }}"
# DATA_DIR = "{{ config_root }}/data"

# ========================================
# Tool Version Management
# ========================================
[tools]

# ========================================
# Runtimes (Language Implementations)
# ========================================

# Node.js - JavaScript/TypeScript runtime
node = "24" # Prefer current LTS major; pin a patch version for strict reproducibility

# Python - Python interpreter
python = "<verified-version>" # Or pin: "3.12.0"

# Ruby - Ruby interpreter
# ruby = "<verified-version>"

# Go - Go language
# go = "<verified-version>"

# Rust - Rust toolchain
# rust = "stable"         # Or "nightly", "1.75.0"

# Lua - Lua interpreter (often needed for Neovim)
# lua = "5.1.5"           # Lua 5.1 for LuaRocks compatibility

# LuaJIT - High-performance Lua
# luajit = "<verified-version>"

# ========================================
# CLI Tools (Standalone Binaries)
# ========================================

# Version Control
github-cli = "<verified-version>" # gh command
# gitlab-cli = "<verified-version>"   # glab command

# Development Tools
# ghq = "<verified-version>"          # Repository manager
# direnv = "<verified-version>"       # Directory-based environment switcher

# Shell/Script Tools
# shellcheck = "<verified-version>"   # Shell script linter
# shfmt = "<verified-version>"        # Shell script formatter

# File/Text Processing
# jq = "<verified-version>"           # JSON processor
# yq = "<verified-version>"           # YAML processor
# ripgrep = "<verified-version>"      # Fast grep (rg)
# fd = "<verified-version>"           # Fast find
# bat = "<verified-version>"          # Cat with syntax highlighting

# Linters/Formatters
# yamllint = "<verified-version>"     # YAML linter
# taplo = "<verified-version>"        # TOML formatter/linter

# Container/Cloud Tools
# docker = "<verified-version>"
# kubectl = "<verified-version>"
# terraform = "<verified-version>"

# ========================================
# NPM Global Packages
# ========================================
# Prefix with "npm:" to install via npm
# These packages are installed in isolated environments per version

# Formatters/Linters
"npm:prettier" = "<verified-version>"
# "npm:eslint" = "<verified-version>"
# "npm:@biomejs/biome" = "<verified-version>"

# Build Tools
# "npm:typescript" = "<verified-version>"
# "npm:vite" = "<verified-version>"
# "npm:webpack" = "<verified-version>"
# "npm:esbuild" = "<verified-version>"

# Development Servers
# "npm:http-server" = "<verified-version>"
# "npm:serve" = "<verified-version>"

# Process Managers
# "npm:pm2" = "<verified-version>"
# "npm:nodemon" = "<verified-version>"

# Testing
# "npm:jest" = "<verified-version>"
# "npm:vitest" = "<verified-version>"

# Package Management
"npm:npm" = "<verified-version>"
# "npm:pnpm" = "<verified-version>"
# "npm:yarn" = "<verified-version>"

# Neovim Integration
# "npm:neovim" = "<verified-version>"                # Node.js client for Neovim
# "npm:@fsouza/prettierd" = "<verified-version>"     # Prettier daemon for faster formatting

# Framework CLIs
# "npm:@angular/cli" = "<verified-version>"
# "npm:@vue/cli" = "<verified-version>"
# "npm:create-react-app" = "<verified-version>"

# Utility Tools
# "npm:npm-check-updates" = "<verified-version>"     # Update package.json dependencies
# "npm:tldr" = "<verified-version>"                  # Simplified man pages

# Protocol Buffers
# "npm:@bufbuild/protoc-gen-es" = "<verified-version>"
# "npm:@connectrpc/protoc-gen-connect-es" = "<verified-version>"

# Documentation
# "npm:markdownlint-cli2" = "<verified-version>"
# "npm:markdown-link-check" = "<verified-version>"

# AI/Automation
# "npm:aicommits" = "<verified-version>"             # AI-powered commit messages
# "npm:@openai/codex" = "<verified-version>"

# ========================================
# Python Global Packages (via pipx)
# ========================================
# Prefix with "pipx:" for isolated Python tool installation
# Each package runs in its own virtual environment

# Formatters/Linters
# "pipx:black" = "<verified-version>"        # Python code formatter
# "pipx:ruff" = "<verified-version>"         # Fast Python linter
# "pipx:pylint" = "<verified-version>"       # Python code analyzer
# "pipx:mypy" = "<verified-version>"         # Static type checker

# Package Management
# "pipx:poetry" = "<verified-version>"       # Python dependency management
# "pipx:pipenv" = "<verified-version>"       # Python virtualenv manager
# "pipx:hatch" = "<verified-version>"        # Modern Python project manager

# Development Tools
# "pipx:ipython" = "<verified-version>"      # Enhanced Python shell
# "pipx:jupyter" = "<verified-version>"      # Jupyter notebooks

# Documentation
# "pipx:mkdocs" = "<verified-version>"       # Static site generator for docs
# "pipx:sphinx" = "<verified-version>"       # Python documentation generator

# Testing
# "pipx:pytest" = "<verified-version>"       # Python testing framework
# "pipx:tox" = "<verified-version>"          # Test automation tool

# HTTP/API Tools
# "pipx:httpie" = "<verified-version>"       # User-friendly HTTP client

# ========================================
# Ruby Gems (if using Ruby)
# ========================================
# "gem:bundler" = "<verified-version>"
# "gem:rails" = "<verified-version>"

# ========================================
# Go Tools (if using Go)
# ========================================
# "go:github.com/golangci/golangci-lint/cmd/golangci-lint" = "<verified-version>"
# "go:golang.org/x/tools/cmd/goimports" = "<verified-version>"

# ========================================
# Cargo Packages (if using Rust)
# ========================================
# "cargo:cargo-watch" = "<verified-version>"
# "cargo:cargo-edit" = "<verified-version>"

# ========================================
# Individual Commands
# ========================================
[tasks]

[tasks.install]
description = "Install all dependencies"
run = "mise install"

[tasks.update]
description = "Update all tools to pinned versions"
run = "mise upgrade"

[tasks.check]
description = "Check mise installation and configuration"
run = "mise doctor"

# ========================================
# Aggregation Tasks
# ========================================

# [tasks."check:format"]
# description = "Check formatting"
# depends = ["format:toml:check", "format:yaml:check"]

# ========================================
# Aliases / Meta Tasks
# ========================================

# [tasks."+ci"]
# description = "Run shared CI checks"
# depends = ["check", "test", "build"]

# ========================================
# Additional Examples
# ========================================

# [tasks.build]
# description = "Build project"
# run = "npm run build"    # Or: cargo build, go build, etc.

# [tasks.clean]
# description = "Clean build artifacts"
# run = "rm -rf dist/ build/ target/"

# ----------------------------------------
# Test Tasks
# ----------------------------------------

# [tasks.test]
# description = "Run tests"
# depends = ["build"]
# run = "npm test"         # Or: cargo test, pytest, go test, etc.

# [tasks.test-watch]
# description = "Run tests in watch mode"
# run = "npm run test:watch"

# ----------------------------------------
# Lint/Format Tasks
# ----------------------------------------

# [tasks.lint]
# description = "Lint code"
# run = "npm run lint"     # Or: cargo clippy, go vet, etc.

# [tasks.format]
# description = "Format code"
# run = "npm run format"   # Or: cargo fmt, gofmt, black, etc.

# [tasks.format-check]
# description = "Check code formatting"
# run = "npm run format:check"

# ----------------------------------------
# CI Tasks
# ----------------------------------------

# [tasks.ci]
# description = "Full CI pipeline"
# depends = ["format-check", "lint", "test", "build"]

# ----------------------------------------
# Alias Examples
# ----------------------------------------

# [tasks.b]
# alias = "build"

# [tasks.t]
# alias = "test"

# [tasks.f]
# alias = "format"

# [tasks."+all"]
# description = "Run all checks"
# depends = ["lint", "test", "build"]

# ========================================
# Hooks (Optional)
# ========================================

# [hooks]
# # Run when entering directory
# enter = "echo 'Welcome to {{ cwd }}'"

# # Run when leaving directory
# leave = "echo 'Goodbye from {{ cwd }}'"

# # Run before any task
# preinstall = "echo 'About to install tools...'"

# # Run after any task
# postinstall = "echo 'Tools installed successfully!'"

# ========================================
# Plugins (Optional)
# ========================================

# [plugins]
# # Custom plugin configuration
# # See: https://mise.jdx.dev/plugins.html

# ========================================
# Usage Examples
# ========================================

# Install all tools:
#   mise install

# Install specific tool:
#   mise install node@24.16.0
#   mise install "npm:prettier@3.8.4"

# List installed tools:
#   mise ls

# Run a task:
#   mise run build
#   mise run test

# Use tool in current shell:
#   mise exec -- node --version
#   mise exec -- prettier --version

# Activate mise in shell (add to .zshrc/.bashrc):
#   eval "$(mise activate zsh)"

# Check configuration:
#   mise doctor

# Update mise itself:
#   mise self-update

# ========================================
# Best Practices
# ========================================

# 1. Centralized Package Management
#    - Declare ALL npm/Python packages in this file
#    - Never use: npm install -g, pip install --user
#    - Rationale: Single source of truth, reproducibility

# 2. Version Pinning
#    - Project-local: Pin specific versions for reproducibility
#    - User global: Use concrete versions for reproducibility
#    - CI/CD: Always pin versions

# 3. Task Organization
#    - Group related tasks with common prefixes
#    - Use `depends` for prerequisites
#    - Keep tasks focused on single responsibility

# 4. Documentation
#    - Add `description` to all tasks
#    - Comment tool choices and version decisions
#    - Keep this file as project documentation

# 5. Version Control
#    - Commit this file to git
#    - Update team when adding new tools
#    - Document any platform-specific requirements
