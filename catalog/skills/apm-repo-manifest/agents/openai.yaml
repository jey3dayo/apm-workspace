interface:
  display_name: "APM Repo Manifest"
  short_description: "Scan a repository and create or clean up its repo-local apm.yml."
  default_prompt: "Scan this repository and prepare a minimal repo-local apm.yml using the APM repo manifest policy."
