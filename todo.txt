(B) 2026-08-12 React/UI validation を利用するリポジトリで repo-local install を検証する +skill-migration @apm
(C) 2026-08-12 understand を次の初見リポジトリで実際に起動するか観察し、使わなければ撤去する +skill-migration @apm
(C) 2026-07-15 社員向けスキル検索 index の最小仕様と apm search 相当の read-only CLI を設計する +skill-search @apm note:docs/skill-inventory.md
(B) 2026-09-04 FalconでGitHub PATのHTTPS利用が再検知された場合に原因経路を特定し不要なtoken送信を止める：Falconの時刻・プロセス・URLを確認し、~/.config/ccstatusline/settings.json:58 の gh pr view、~/.config/zsh/lib/mise.zsh:8-10 の gh auth token -> MISE_GITHUB_TOKEN、~/.apm/manual-skills の gh api/gh 呼び出し、APMメタデータ・ポリシー取得を照合する。完了条件：原因経路を特定し、不要な自動呼び出しを停止または承認済み経路に変更し、PATの平文ファイル不在とSSH fetchを再確認する +security @apm @config
(B) 2026-09-04 mise run check が agent frontmatter の YAML 妥当性を検証していない：description に `: `（コロン+空白）を含む引用符なしスカラーを書いても check が通り、5ファイルが壊れたまま gate を通過した。validate:catalog に frontmatter パースを追加し、name/description の存在も確認する +apm @apm
(C) 2026-09-04 code-reviewer と thermo-nuclear-code-quality-review の使い分けを description に書く：ブラインド判定で「この実装の設計に問題ないか見て」が両者で競合した。thermo-nuclear は「Invoked via Task after a parent gathers diff and file contents」という前提条件だけで、通常の親セッションがいつ選ぶかの手がかりが無い。単発の設計相談は code-reviewer、diff 一式を渡す本格監査は thermo-nuclear、という切り分けを両方へ入れる +prompt-audit @apm
(C) 2026-09-04 コードロケーション依頼の受け皿を description で切り分ける：ブラインド判定で「認証周りの処理ってどこにある？」が deep-explore / 組み込み Explore / researcher の3者で判別不能だった。symbol を名指しできるかで deep-explore と serena は分けたが、範囲が曖昧な探索を Explore と researcher のどちらへ渡すかの基準が無い +prompt-audit @apm
(B) 2026-09-04 Codex worker に minimal MCP profile を用意する：実運用で Codex worker が global MCP を引き継ぎ Voicevox/Context7/CUA/memory lookup 等を起動した。leaf implementation に不要で startup・context・process 数が増える。Claude helper の empty MCP 相当を run-codex-worker.sh へ入れる +skill-refactor @apm note:CyMaster PR892 feedback #3
(C) 2026-09-04 agmsg の join/identities/reset の canonicalization 統一を upstream へ出す：join.sh は worktree path を repo root へ正規化するが identities.sh に worktree path を渡すと空を返し、Lifecycle の exact identity verification が成立しない。agmsg は外部パッケージ(fujibee/agmsg)なので agmsg-delegation 側には回避策のみ記載済み +upstream @apm note:CyMaster PR892 feedback #4
(C) 2026-09-04 launch-worker.sh の run_dir 拒否エラーに復旧コマンドを添える：/tmp/agmsg-delegation.* は canonical /private/tmp になり拒否される。エラー文に accepted な mktemp コマンドを出す +skill-refactor @apm note:CyMaster PR892 feedback #6
(C) 2026-09-04 review payload template に既存 CI 証拠の活用を書く：reviewer が CI 証拠があるのに broad test 再実行と環境 workaround を重ね、node_modules 欠落で vitest 74件 import failure まで踏んだ。「既存 CI 結果を確認し追加テストは finding 再現に限定」「repo target 優先」を入れる。role-scoped writable cache(GOCACHE 等)の注入も検討 +skill-refactor @apm note:CyMaster PR892 feedback #7 #8
(C) 2026-09-04 委譲判定で source files と generated outputs を分けて数える：authoritative source 2 files + mechanical generation 5 files が「3ファイル以上」に当たり軽作業まで Luna xhigh 委譲になった。生成物のみのファイルを除外するか別カウントにする。あわせて Worker の commit 禁止と repo 固有の verify-generated(generated paths が dirty だと必ず失敗)の責務分担を明記する +skill-refactor @apm note:CyMaster PR892 feedback #9 #10
