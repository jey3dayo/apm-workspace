(C) 2026-09-04 FalconでGitHub PATのHTTPS利用が再検知された場合に原因経路を特定し不要なtoken送信を止める：Falconの時刻・プロセス・URLを確認し、~/.config/ccstatusline/settings.json:58 の gh pr view、~/.config/zsh/lib/mise.zsh:8-10 の gh auth token -> MISE_GITHUB_TOKEN、~/.apm/manual-skills の gh api/gh 呼び出し、APMメタデータ・ポリシー取得を照合する。再開条件：セキュリティチームからの連絡（2026-09-05にサスペンド、それまで着手しない）。完了条件：原因経路を特定し、不要な自動呼び出しを停止または承認済み経路に変更し、PATの平文ファイル不在とSSH fetchを再確認する +security @apm @config @waiting
(B) 2026-09-05 catalogへrepo固有の値が入るのを既存checkで検出する：catalog/agentsとcatalog/skillsの本文・references・runtime assetsの追加変更を対象に、実値入り--profile、ARN・account ID、具体的なcluster/service/domain、repo専用path、pnpm/mise script、存在しないagent委譲先を抽出する。曖昧な候補はfile:line付き警告に留め、機械的に確認できる未解決参照とrepo固有の実行依存が確定したものだけ失敗させる。pre-push/CIのcheckとdeploy前checkへ接続し、CIは差分・配布直前はbaseline比較とする。完了条件：本日廃止したagent3件の旧内容を回帰fixtureとして検出でき、汎用AWS skillのダミー例が通ることを確認し、mise run checkに組み込まれている +apm +security @apm
(B) 2026-09-05 catalogからproject repoへの移設に有効化前gateを定める：移設先で有効化される前に、認証/profile・環境リソース識別子・実行script・委譲先・参照pathの契約を移設先の正本と全量照合し、本番操作を含む移設は作者と別のReviewerが有効化前に確認する。.claude/agentsのような自動読込先はコピー自体が有効化にあたるため、isolated branch/worktreeの固定候補でレビューする。完了条件：手順がapm-usageまたはcatalog/AGENTS.mdに書かれ、照合結果の記録形式（記述→移設先の正本path/行→一致/修正/未確認）が定義されている +apm @apm
(C) 2026-09-05 caad-astaへ移設した3 agentから変動値の複製を減らす：cluster名・profile・script名などをagent本文に持たず、repo側の運用skillと実行スクリプトを参照する形へ寄せる。移設時のgateだけではrepo-local化後の再腐敗を防げないため。完了条件：本文の運用値がrepo側正本の参照に置き換わり、terraformとの照合が不要な状態になっている @caad-asta
