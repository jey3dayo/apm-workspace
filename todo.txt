(B) 2026-09-04 FalconでGitHub PATのHTTPS利用が再検知された場合に原因経路を特定し不要なtoken送信を止める：Falconの時刻・プロセス・URLを確認し、~/.config/ccstatusline/settings.json:58 の gh pr view、~/.config/zsh/lib/mise.zsh:8-10 の gh auth token -> MISE_GITHUB_TOKEN、~/.apm/manual-skills の gh api/gh 呼び出し、APMメタデータ・ポリシー取得を照合する。完了条件：原因経路を特定し、不要な自動呼び出しを停止または承認済み経路に変更し、PATの平文ファイル不在とSSH fetchを再確認する +security @apm @config
(B) 2026-09-04 Codex worker に minimal MCP profile を用意する：実運用で Codex worker が global MCP を引き継ぎ Voicevox/Context7/CUA/memory lookup 等を起動した。leaf implementation に不要で startup・context・process 数が増える。調査済み：MCP 定義は ~/.codex/config.toml の [mcp_servers.*] に12個あり、`codex -c mcp_servers={}` では上書きできない（codex mcp list に12件そのまま残る）。効く機構の特定が先で、per-server enabled=false の一括指定か profile 側での置換か undocumented flag を当たる。Claude helper の empty MCP + --strict-mcp-config が参考 +skill-refactor @apm note:CyMaster PR892 feedback #3
