(C) 2026-09-04 FalconでGitHub PATのHTTPS利用が再検知された場合に原因経路を特定し不要なtoken送信を止める：Falconの時刻・プロセス・URLを確認し、~/.config/ccstatusline/settings.json:58 の gh pr view、~/.config/zsh/lib/mise.zsh:8-10 の gh auth token -> MISE_GITHUB_TOKEN、~/.apm/manual-skills の gh api/gh 呼び出し、APMメタデータ・ポリシー取得を照合する。再開条件：セキュリティチームからの連絡（2026-09-05にサスペンド、それまで着手しない）。完了条件：原因経路を特定し、不要な自動呼び出しを停止または承認済み経路に変更し、PATの平文ファイル不在とSSH fetchを再確認する +security @apm @config @waiting
x 2026-09-06 2026-09-05 caad-astaへ移設した3 agentから変動値の複製を減らす：cluster名・profile・script名などをagent本文に持たず、repo側の運用skillと実行スクリプトを参照する形へ寄せる。移設時のgateだけではrepo-local化後の再腐敗を防げないため。完了条件：本文の運用値がrepo側正本の参照に置き換わり、terraformとの照合が不要な状態になっている @caad-asta
(C) 2026-09-07 caad-asta の monitoring-alerts.md と terraform-operations.md から変動値の複製を減らす：2026-09-06 に done.txt へ「3 agent から変動値の複製を減らす」を完了記録したが、実態は 3 つのうち aws-operations.md だけで、monitoring-alerts.md に 8 箇所、terraform-operations.md に 17 箇所の app_name 焼き込みが残っている（2026-09-07 に main-cc が照合）。app_name は terraform の変数（terraform/environments/*/shared_variables.tf の default "asta"）なので変更時に 25 箇所が腐る。焼き込みは 3 種類に分かれる: (1) terraform に正本があり参照化できるもの（cluster_name / service_name / backend bucket）、(2) CLI 実行例に埋まっており参照化するとコピペ不能になるもの、(3) app_name 無関係で対象外にすべきもの（asta-deployment は agent 名、asta-stg は DNS ホスト名で terraform から導出不可）。作業は caad-asta 側の asta-cc へ引き継ぎ済み（task_id caad-asta-variable-values-0907、判断 Q1-Q3 を送付）。完了条件：caad-asta 側で (1) が参照化され、(2) の方針が本文または注記に記録され、terraform との照合が不要な状態になっている。main-cc 側の作業はこの起票と引き継ぎで完了 @caad-asta @handoff
(C) 2026-09-07 agmsg の send.sh が差出人を検証しない件を upstream へ出すか手前でガードするか決める：ultra-rss-reader から「actas の排他が session_id 重複で崩れる」という報告が来たが、差出人自身が 2026-09-07 に全面撤回した（agmsg 1.2.3 は actas-lock.sh:30 のとおり <session_id>.<pid> の複合 token を所有者に使い #93 として対処済み。実ロック ~/.agents/skills/agmsg/run/actas.apm__steward-t3.session も複合形を確認）。撤回後に有効なまま残るのは 1 点で、send.sh は差出人名を引数で受けるだけで、その名前の actas ロックを自分が保持しているか検証しない。ロックが存在しなければ誰でもその identity を名乗って送信できる（ultra-rss 側で実際に起きた原因はロック不在であって ID 重複ではない）。agmsg は apm.yml:73 の外部依存 fujibee/agmsg なので本リポジトリでは直せない。完了条件：upstream へ issue を出して URL を本項目へ残すか、手前でガードする方針を決めて catalog 側へ記録するか、どちらかが済んでいる +agmsg @apm @upstream
(C) 2026-09-07 公開した Artifact でクリップボード画像コピーが動くか確認する：docs/diagrams/orchestrator-worker.html を Artifact として公開し、エクスポート（PNG/JPEG/WebP/SVG/WebM と share-card）は downloads capability 経由へ差し替えて閲覧者側でも動くようにしたが、Copy diagram / Copy Share Card が使う navigator.clipboard.write + ClipboardItem は未確認。clipboard 専用の capability は存在せず、artifact の iframe に clipboard-write が許可されているかで決まる。完了条件：https://claude.ai/code/artifact/dc228850-6522-4b49-939e-d875319c91d0 を開いて Copy diagram を押し、成否を本項目へ記録する。動かない場合はボタンを隠すか downloads へ寄せるかを決める +artifact @apm
