(C) 2026-09-04 FalconでGitHub PATのHTTPS利用が再検知された場合に原因経路を特定し不要なtoken送信を止める：Falconの時刻・プロセス・URLを確認し、~/.config/ccstatusline/settings.json:58 の gh pr view、~/.config/zsh/lib/mise.zsh:8-10 の gh auth token -> MISE_GITHUB_TOKEN、~/.apm/manual-skills の gh api/gh 呼び出し、APMメタデータ・ポリシー取得を照合する。再開条件：セキュリティチームからの連絡（2026-09-05にサスペンド、それまで着手しない）。完了条件：原因経路を特定し、不要な自動呼び出しを停止または承認済み経路に変更し、PATの平文ファイル不在とSSH fetchを再確認する +security @apm @config @waiting
x 2026-09-06 2026-09-05 caad-astaへ移設した3 agentから変動値の複製を減らす：cluster名・profile・script名などをagent本文に持たず、repo側の運用skillと実行スクリプトを参照する形へ寄せる。移設時のgateだけではrepo-local化後の再腐敗を防げないため。完了条件：本文の運用値がrepo側正本の参照に置き換わり、terraformとの照合が不要な状態になっている @caad-asta
(B) 2026-09-07 `apm.lock.yaml` の意図しない resolved_commit 移動の発生契機を特定する：2026-09-07 の `mise run deploy` 後に 13 パッケージの `resolved_commit` が動いた（`generated_at: 2026-09-07T01:19:36Z`）。内容は 2026-09-07 にレビュー済みで受け入れ済み（artifact 削除 0 件、パッケージ増減なし、実行資材 3 件の hash 変化は catalog 正本と一致＝自リポジトリの過去コミット由来、高リスクなし）。実測: `mise run apply` / `mise run check` / `mise run doctor` の各単体再実行では lock は動かない。仮説（未確認）: mobbin を `apm.yml` へ追加した直後の install が unpinned な floating ref を一斉に再解決した一回性の効果。この仮説を `AGENTS.md:48` の "Preserves manifest and lock" の但し書きへ昇格させないこと。完了条件：発生契機が実測で特定され、`AGENTS.md:48` の記述を維持するか実挙動へ合わせるかが決まっている。調査は既存 install ログと導入済み APM のソース確認を先に行い、必要なら旧 manifest / 旧 lock の隔離 fixture で再現する（実 workspace の manifest 変更を必須にしない。キャッシュや global 配布先を共有する fixture では隔離にならない） +apm @lockfile
