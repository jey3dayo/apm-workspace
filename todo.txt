(C) 2026-09-04 FalconでGitHub PATのHTTPS利用が再検知された場合に原因経路を特定し不要なtoken送信を止める：Falconの時刻・プロセス・URLを確認し、~/.config/ccstatusline/settings.json:58 の gh pr view、~/.config/zsh/lib/mise.zsh:8-10 の gh auth token -> MISE_GITHUB_TOKEN、~/.apm/manual-skills の gh api/gh 呼び出し、APMメタデータ・ポリシー取得を照合する。再開条件：セキュリティチームからの連絡（2026-09-05にサスペンド、それまで着手しない）。完了条件：原因経路を特定し、不要な自動呼び出しを停止または承認済み経路に変更し、PATの平文ファイル不在とSSH fetchを再確認する +security @apm @config @waiting
(C) 2026-09-07 `audit:ci:smoke` の設計不整合を解消する：`scripts/apm-workspace.sh` の `cmd_audit_ci_smoke` は temp dir へ apm.yml / apm.lock.yaml / apm-policy.yml だけを複製して `apm install --only apm` → `apm audit --ci` を実行するが、audit の `deployed-files-present` チェックは lockfile の deployment ledger に載る実配布先ファイル（`.agents/skills/**`、workspace-only / private / manual lane を含む）の存在を検査するため、配布を持たない temp workspace では原理的に通らない。2026-09-07 実測：`110 deployed files missing`、他 4 チェック（lockfile-exists / ref-consistency / deployment-ledger-owners / drift）は pass。`apm audit --help`（0.29.0）に当該チェックを除外するフラグは無い。`lefthook.yml` の pre-commit / pre-push からは呼ばれておらず gate になっていないため、実害は「手で叩くと必ず落ちる orphan task」に留まる。当初 shim 解決の失敗と誤診したが、`mise run` 経由なら install は通る（直接 `bash scripts/...` で叩いた場合のみ shim が解決できない）。選択肢：(a) audit 対象を temp ではなく `$WORKSPACE_DIR` にする（2026-09-07 実測：実 workspace では ref-consistency / lockfile-exists / deployment-ledger-owners は pass するが `deployed-files-present` が 10 件、`drift` が 2719 件で落ちる。いずれも .cursor/ .opencode/ 系と legacy artifact で、CLAUDE.md が非問題と定める class に該当するため、(a) を採るならこの 2 チェックの扱いを先に決める必要がある） (b) temp install で manifest が解決することの確認だけに縮小し `apm audit --ci` を外す（同等の確認は `apm install --dry-run` と pre-push の `validate:workspace` が既に担う） (c) gate になっていないタスクとして撤去する。対象：scripts/apm-workspace.sh、scripts/apm-workspace.ps1、mise/test.toml:16-19。完了条件：選択肢のいずれかを採用し、`mise run audit:ci:smoke` が exit 0 で終わるか、タスク自体が無くなっている +apm @task @decision
(C) 2026-09-07 apm 上流へ `_allows_missing_manifest` の意図と条件のズレを報告する：apm 0.29.0 の `apm_cli/integration/mcp_config_view.py:209-239` は apm.yml を持たないパッケージの免除判定に `dependency_ref.is_virtual_subdirectory()` を使うが、同ファイル 246 行のコメントは意図を "a virtual claude_skill legitimately ships no apm.yml by design" と書いており、リポジトリ root 自体が skill である claude_skill（package_type: claude_skill かつ 2 segment ref）が免除から漏れる。影響：`apm install --frozen` が gonta223/humanizer-ja を out-of-sync として拒否するため、この workspace では `--frozen` を install 系 task へ配線できない（2026-09-07 実測。marketplace_plugin の 4 件は deps/plugin_parser.py が apm.yml を合成するので免除関数へ到達せず影響しない）。報告は外部 OSS への外向き行動なのでユーザーの明示確認が必要。対象：apm upstream の issue（このリポジトリのファイルは変更しない）。完了条件：upstream へ issue を出したか、報告しないという判断を理由付きで記録した +apm @upstream @waiting
