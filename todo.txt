(C) 2026-09-04 FalconでGitHub PATのHTTPS利用が再検知された場合に原因経路を特定し不要なtoken送信を止める：Falconの時刻・プロセス・URLを確認し、~/.config/ccstatusline/settings.json:58 の gh pr view、~/.config/zsh/lib/mise.zsh:8-10 の gh auth token -> MISE_GITHUB_TOKEN、~/.apm/manual-skills の gh api/gh 呼び出し、APMメタデータ・ポリシー取得を照合する。再開条件：セキュリティチームからの連絡（2026-09-05にサスペンド、それまで着手しない）。完了条件：原因経路を特定し、不要な自動呼び出しを停止または承認済み経路に変更し、PATの平文ファイル不在とSSH fetchを再確認する +security @apm @config @waiting
x 2026-09-06 2026-09-05 caad-astaへ移設した3 agentから変動値の複製を減らす：cluster名・profile・script名などをagent本文に持たず、repo側の運用skillと実行スクリプトを参照する形へ寄せる。移設時のgateだけではrepo-local化後の再腐敗を防げないため。完了条件：本文の運用値がrepo側正本の参照に置き換わり、terraformとの照合が不要な状態になっている @caad-asta
(B) 2026-09-07 `mise run deploy` が `apm.lock.yaml` を保持していない：`AGENTS.md:48` は "Stable rollout / mise run deploy / Preserves manifest and lock." と定めているが、2026-09-07 の deploy 実行後に `resolved_commit` が 28 件動いた（`generated_at: 2026-09-07T01:19:36Z`、対象は frontend-design / agentation / browser-harness / diagram-design など unpinned な外部パッケージ）。経路は `cmd_apply` の `install_workspace_mcp_dependencies`（`apm install -g --only mcp`）が manifest 全体を再解決している疑い。完了条件：deploy が lock を動かす経路が特定され、契約どおり保持するか、あるいは AGENTS.md の記述を実挙動へ合わせるかが決まっている。動いた 28 件の受け入れ可否は別判断として切り出す +apm @lock
