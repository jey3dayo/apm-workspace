(C) 2026-09-11 APM が catalog から retired された skill の deployed_files 記録を落とさない件を切り分ける：`apm.lock.yaml` の `jey3dayo/apm-workspace` レコード（5393行目）配下に `.agents/skills/work-log-maintenance` の記録が11行残る（deployed_files 4 / hash 3 / locator 4、locator の active_owner は `jey3dayo/apm-workspace/catalog`）。実体は private レーン（`private-skills/.apm/skills/work-log-maintenance/`）へ移っており、上流 catalog にもパッケージ実体にも存在しない（`git cat-file -e origin/main:catalog/skills/work-log-maintenance/SKILL.md` → does not exist、`apm_modules/jey3dayo/apm-workspace/catalog/skills/work-log-maintenance` も不在）。症状は `apm install` のたびに出る `Skipped removing ... file has been edited since APM deployed it`。2026-09-11 に3手試して全て不発：(1) `apm lock -g` → 差分ゼロ（deployed_files / locator は解決結果でなく配布実績の記録なので動かない） (2) 配布実体を退避して `apm install -g --only apm` → 記録落ちず、かつ当時未 push だった `check-env-secrets.sh` が配布先から消える副作用 (3) push 後の `mise run install:catalog` → exit 1（`Local package cache is stale`）。実害は表示ノイズと lock 上の owner 誤認のみで、配布内容は private 正本と `diff -rq` 一致。対象：`catalog/skills/apm-usage/`（仕様だった場合）または apm upstream の issue（不具合だった場合）。完了条件：APM CLI が retired package の deployed_files を意図的に保持する仕様か否かを一次情報で確定し、仕様なら `apm-usage` の Guardrails へ既知ノイズとして記録、不具合なら上流へ起票する。判定後は `command grep -c 'work-log-maintenance' apm.lock.yaml` が 0 になるか、仕様として記録済みであること +apm @task @decision @waiting 2026-09-23 追加事例: 撤去した quiet-command-auditor の .agents / .config/opencode 向け配布記録が、pin を cbb1064 へ移した後（202e8b5）も lock に残った（.claude 向けは消えた）。実体は無い 2026-09-24 調査：work-log-maintenance の記録が残るのは仕様。apm 0.29.0 の integration/cleanup.py にある gate 3 は、記録した hash と実体が違うと削除も記録の除去もしない（PR #1767 以来の意図された挙動）。実体は private lane の中身と一致し、catalog 時代の hash と違うので毎回 skip される。解消するには実体を消してから install を通し、そのあと private lane から再配布する（配布先の削除はユーザーの許可待ち）。一方 quiet-command-auditor は実体が無いのに記録が残り、cleanup.py の「ファイルが無ければ片付け済みとして扱う」と合わない。こちらは未解明で、移行済み target の記録が reconcile されない（#2139 系）可能性がある 2026-09-24 原因確定（apm 0.31.0）：上流 APM の不具合。lock の deployments: にある target ラベルで行を消すかが決まり、core/deployment_state.py:404-415 の _is_stale は有効な target 名か既知の target 名のときしか消さない。.agents/ の行（1574 行）には 0.28/0.29 が付けた agents ラベル、.config/ の行（1223 行）には legacy ラベルが付いており、どちらも整理されない。catalog だけで実体の無い行が 274 行あり、今後 catalog から skill を消すたびに増える。上流の関連は #2743（PR #2774 で agents を legacy に変更）と #2902（PR #2924 は merge 済みだが未リリース）。既存ラベルを移行する修正は無い。回避策は scratch で実測済みで、deployments: の .agents/ 行のラベルを codex へ手で書き換えてから全体 install をやり直す（lock の手編集で全依存の配置をやり直すうえ、agmsg の save/restore が要る）。影響は警告と lock のノイズだけなので未実施。再開条件：#2924 を含む apm のリリースで改善するかを確かめるか、ユーザーが手編集の実施か上流への報告を指示したとき
(C) 2026-09-18 bats が run tmpdir を消せず残す症状の原因を特定する：`mise run test` の `test:sh` が稀に `rm: $TMPDIR/bats-run-XXXXXX/test: Directory not empty` を出して run tmpdir を残す。テスト結果には影響しない（not ok は常に 0 件、CI も緑）。観測は 2026-09-18 12:59 の `bats-run-eDyyI0`（1 エントリ残存）と 2026-09-15 12:04 の `bats-run-hHY2rA`（2260 エントリ残存）の 2 回で、初発ではない。2026-09-18 に候補を 3 つ実測で外した：(1) 同時に出ていた `test:ps` の file レベル FAILED は別原因で、Pester の `-CI` が有効にする TestResult の既定 OutputPath が相対 `testResults.xml` のため並列プロセスが同一ファイルを掴み合っていた（`429db80` / `7f875e3` で解消） (2) プロセス上限は無関係（事象当時 988 / 上限 10666） (3) 修正後の full gate 約 10 回（逐次・2 並列とも）で再現しない。唯一発生した回の出力を `tail` で切って失っており、ログが無いまま parallelism や cleanup を触ると当て推量になる。再開条件：次に症状が出たとき（それまで着手しない）。そのとき `mise run test` の出力を丸ごとファイルへ保存し（`> log 2>&1` で取り `tail` で切らない）、同時に `pgrep -fl "bats|pwsh"` を記録して並行セッションの有無を残す。対象：`mise/test.toml` の `test:sh`、`scripts/run-pester.sh`。完了条件：再現ログから原因を特定して cleanup を直したか、仕様として `AGENTS.md` へ既知ノイズとして記録したか、証拠を破棄する判断を理由付きで記録した +apm @task @waiting 2026-09-24 調査：証拠の bats-run-hHY2rA は既に無い。原因として有力なのは bats-core 1.14.0 の --jobs の semaphore で、touch と flock/shlock による slot 確保が非 atomic。upstream の PR #1259（2026-09-20 merge、atomic な mkdir へ置換、macOS で散発する並列失敗を明記）が未リリースの修正にあたる（因果は推定）。再開条件に「#1259 を含む bats リリース」を加え、リリースされたら mise の pin を上げて様子を見る
(C) 2026-09-25 mise run upgrade の封印を解く条件を満たしたら再開する：apm 0.31.0 の apm update は SHA pin を最新タグへ書き換え、タグのほうが古くても戻す（2026-09-25 の dry-run で8件中6件が後退）。同じ SHA を共有する複数行の書き換えは duplicate-pin で止まる。同じ SHA の複数行は 2026-09-25 に skills: subset で畳んだので duplicate-pin は踏まない（残るのは caad-develop/claude-code-marketplace の2行で、タグが無く update 対象外）。経緯は docs/package-decisions.md の同日の節。対象：mise.toml の tasks.upgrade（前進だけ適用する形）。再開条件：pin より古いタグへ戻さない apm のリリース、またはユーザーが upgrade の置き換えを指示したとき。完了条件：mise run upgrade が後退なしで通るか、upgrade を前進だけ適用する形へ置き換えた +apm @task @waiting
