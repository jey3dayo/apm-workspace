(C) 2026-09-18 bats が run tmpdir を消せず残す症状の原因を特定する：`mise run test` の `test:sh` が稀に `rm: $TMPDIR/bats-run-XXXXXX/test: Directory not empty` を出して run tmpdir を残す。テスト結果には影響しない（not ok は常に 0 件、CI も緑）。観測は 2026-09-18 12:59 の `bats-run-eDyyI0`（1 エントリ残存）と 2026-09-15 12:04 の `bats-run-hHY2rA`（2260 エントリ残存）の 2 回で、初発ではない。2026-09-18 に候補を 3 つ実測で外した：(1) 同時に出ていた `test:ps` の file レベル FAILED は別原因で、Pester の `-CI` が有効にする TestResult の既定 OutputPath が相対 `testResults.xml` のため並列プロセスが同一ファイルを掴み合っていた（`429db80` / `7f875e3` で解消） (2) プロセス上限は無関係（事象当時 988 / 上限 10666） (3) 修正後の full gate 約 10 回（逐次・2 並列とも）で再現しない。唯一発生した回の出力を `tail` で切って失っており、ログが無いまま parallelism や cleanup を触ると当て推量になる。再開条件：次に症状が出たとき（それまで着手しない）。そのとき `mise run test` の出力を丸ごとファイルへ保存し（`> log 2>&1` で取り `tail` で切らない）、同時に `pgrep -fl "bats|pwsh"` を記録して並行セッションの有無を残す。対象：`mise/test.toml` の `test:sh`、`scripts/run-pester.sh`。完了条件：再現ログから原因を特定して cleanup を直したか、仕様として `AGENTS.md` へ既知ノイズとして記録したか、証拠を破棄する判断を理由付きで記録した +apm @task @waiting 2026-09-24 調査：証拠の bats-run-hHY2rA は既に無い。原因として有力なのは bats-core 1.14.0 の --jobs の semaphore で、touch と flock/shlock による slot 確保が非 atomic。upstream の PR #1259（2026-09-20 merge、atomic な mkdir へ置換、macOS で散発する並列失敗を明記）が未リリースの修正にあたる（因果は推定）。再開条件に「#1259 を含む bats リリース」を加え、リリースされたら mise の pin を上げて様子を見る
(C) 2026-09-25 mise run upgrade の封印を解く条件を満たしたら再開する：apm 0.31.0 の apm update は SHA pin を最新タグへ書き換え、タグのほうが古くても戻す（2026-09-25 の dry-run で8件中6件が後退）。同じ SHA を共有する複数行の書き換えは duplicate-pin で止まる。同じ SHA の複数行は 2026-09-25 に skills: subset で畳んだので duplicate-pin は踏まない（残るのは caad-develop/claude-code-marketplace の2行で、タグが無く update 対象外）。経緯は docs/package-decisions.md の同日の節。対象：mise.toml の tasks.upgrade（前進だけ適用する形）。再開条件：pin より古いタグへ戻さない apm のリリース、またはユーザーが upgrade の置き換えを指示したとき。完了条件：mise run upgrade が後退なしで通るか、upgrade を前進だけ適用する形へ置き換えた +apm @task @waiting
