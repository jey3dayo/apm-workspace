(C) 2026-09-04 FalconでGitHub PATのHTTPS利用が再検知された場合に原因経路を特定し不要なtoken送信を止める：Falconの時刻・プロセス・URLを確認し、~/.config/ccstatusline/settings.json:58 の gh pr view、~/.config/zsh/lib/mise.zsh:8-10 の gh auth token -> MISE_GITHUB_TOKEN、~/.apm/manual-skills の gh api/gh 呼び出し、APMメタデータ・ポリシー取得を照合する。再開条件：セキュリティチームからの連絡（2026-09-05にサスペンド、それまで着手しない）。完了条件：原因経路を特定し、不要な自動呼び出しを停止または承認済み経路に変更し、PATの平文ファイル不在とSSH fetchを再確認する +security @apm @config @waiting
(C) 2026-09-07 `audit:ci:smoke` の設計不整合を解消する：`scripts/apm-workspace.sh` の `cmd_audit_ci_smoke` は temp dir へ apm.yml / apm.lock.yaml / apm-policy.yml だけを複製して `apm install --only apm` → `apm audit --ci` を実行するが、audit の `deployed-files-present` チェックは lockfile の deployment ledger に載る実配布先ファイル（`.agents/skills/**`、workspace-only / private / manual lane を含む）の存在を検査するため、配布を持たない temp workspace では原理的に通らない。2026-09-07 実測：`110 deployed files missing`、他 4 チェック（lockfile-exists / ref-consistency / deployment-ledger-owners / drift）は pass。`apm audit --help`（0.29.0）に当該チェックを除外するフラグは無い。`lefthook.yml` の pre-commit / pre-push からは呼ばれておらず gate になっていないため、実害は「手で叩くと必ず落ちる orphan task」に留まる。当初 shim 解決の失敗と誤診したが、`mise run` 経由なら install は通る（直接 `bash scripts/...` で叩いた場合のみ shim が解決できない）。選択肢：(a) audit 対象を temp ではなく `$WORKSPACE_DIR` にする（2026-09-07 実測：実 workspace では ref-consistency / lockfile-exists / deployment-ledger-owners は pass するが `deployed-files-present` が 10 件、`drift` が 2719 件で落ちる。いずれも .cursor/ .opencode/ 系と legacy artifact で、CLAUDE.md が非問題と定める class に該当するため、(a) を採るならこの 2 チェックの扱いを先に決める必要がある） (b) temp install で manifest が解決することの確認だけに縮小し `apm audit --ci` を外す（同等の確認は `apm install --dry-run` と pre-push の `validate:workspace` が既に担う） (c) gate になっていないタスクとして撤去する。対象：scripts/apm-workspace.sh、scripts/apm-workspace.ps1、mise/test.toml:16-19。完了条件：選択肢のいずれかを採用し、`mise run audit:ci:smoke` が exit 0 で終わるか、タスク自体が無くなっている +apm @task @decision
(C) 2026-09-07 apm 上流へ `_allows_missing_manifest` の意図と条件のズレを報告する：apm 0.29.0 の `apm_cli/integration/mcp_config_view.py:209-239` は apm.yml を持たないパッケージの免除判定に `dependency_ref.is_virtual_subdirectory()` を使うが、同ファイル 246 行のコメントは意図を "a virtual claude_skill legitimately ships no apm.yml by design" と書いており、リポジトリ root 自体が skill である claude_skill（package_type: claude_skill かつ 2 segment ref）が免除から漏れる。影響：`apm install --frozen` が gonta223/humanizer-ja を out-of-sync として拒否するため、この workspace では `--frozen` を install 系 task へ配線できない（2026-09-07 実測。marketplace_plugin の 4 件は deps/plugin_parser.py が apm.yml を合成するので免除関数へ到達せず影響しない）。報告は外部 OSS への外向き行動なのでユーザーの明示確認が必要。対象：apm upstream の issue（このリポジトリのファイルは変更しない）。完了条件：upstream へ issue を出したか、報告しないという判断を理由付きで記録した +apm @upstream @waiting
(B) 2026-09-08 OpenCode の global skills 配布先が workspace adapter と upstream で食い違っているのを解消する：`scripts/apm-workspace.sh:1618` の `managed_catalog_runtime_targets` は `opencode|.opencode` を定義し `:2353-2365` の `replace_skill_targets_from_stage` が `~/.opencode/skills` を書き換えるが、導入済み apm 0.29.0 の `apm_cli/integration/targets.py:634-655` は OpenCode の `user_root_dir=".config/opencode"` と `user_primitive_overrides["skills"]` を定義しており、user scope の skills は `~/.config/opencode/skills` が正。公式 docs（https://opencode.ai/docs/skills/）も global は `~/.config/opencode/skills`、`.opencode/skills` は project config と記載。2026-09-08 実測：`orchestrator-worker` を変更して `mise run deploy:fresh`（exit 0）した後、catalog / `~/.claude` / `~/.agents` / `~/.opencode` の SKILL.md は sha256 `b8393482…` で一致し削除した `references/codex-spawn-model-bug.md` も不在だが、`~/.config/opencode/skills` は sha256 `4d3c9bf1…`（2026-09-06 時点）のままで削除済み reference も残存。catalog 29 skill のうち 6 件（agmsg-delegation / apm-repo-manifest / ci-stability-hooks / orchestrator-worker / similarity / todo-changelog-ops）が同様に stale。`apm.lock.yaml` は `.config/opencode/skills/**` を 1431 entry で追跡し（`.opencode/skills/**` は 0 件）旧 sha のまま再生成されていない。なお deploy ログの `Skipped workspace-only runtimes at user scope: cursor, opencode` は `mcp_integrator_install.py:742-747` が出す MCP 専用の警告で、skills の scope 判定の根拠にはならない（当初これを根拠に「対象外」と誤判定した）。OpenCode は `~/.claude/skills` と `~/.agents/skills` も探索するため実害は限定的だが、stale な旧版が探索対象に残る。同名 skill の precedence は未検証。完了条件：(1) shell / PowerShell adapter と upstream の user/project target mapping を照合する (2) OpenCode の実効 global root と同名 skill の解決元を確認する (3) 変更 skill の全文・補助ファイル・削除の absence が正本と一致する (4) lock の所有記録と adapter の記録を整合させる (5) unrelated / manual / external skill（`~/.config/opencode/skills` は 188 entry あり `~/.opencode/skills` の 76 entry より多い）を壊さず移行する (6) `doctor` と `apm-deploy-verify` の検証を正しい root へ向ける。`.apm/skills/apm-deploy-verify/SKILL.md:20` の手順 3 は `~/.claude` と `~/.agents` しか比較しておらず OpenCode global root を検証していないので、同じ別件で塞ぐ。`~/.opencode/skills` の撤去は inventory 後の別判断とし、この作業では消さない +apm @deploy @decision
(C) 2026-09-08 `scripts/apm-workspace.sh` が apm.yml の `alias:` を無視して配布名を決めるのを直す：apm 0.29.0 は object 形式の dependency で `alias: <name>` を受け付け、`apm install --target claude` は `.claude/skills/<alias>/` へ配置する（2026-09-08 に scratchpad で実測）。lockfile も `name: japanese-tech-writing` として alias を記録する。しかし `scripts/apm-workspace.sh` には `alias` の出現が 0 件で、`:1852-1875` の解決は lockfile の `repo_url` と `virtual_path` だけを見る。このため `mise run apply` 経由の rollout では alias が効かず、gist 依存（`https://gist.github.com/k16shikano/fd287c3133457c4fd8f5601d34aa817d.git`）を入れたときに 3 配布面すべてへ `fd287c3133457c4fd8f5601d34aa817d` という gist ID 名で配置された（2026-09-08 実測、その場で差し戻し済み）。影響：repo 名が skill 名として不適な upstream（gist、単一 skill を持つ汎用名 repo）を外部依存にできず、catalog へ vendor するしかない。実例が `japanese-tech-writing`（`docs/package-decisions.md` 参照）。完了条件：`mise run apply` 経由の rollout が lockfile の alias（`name:` フィールド）を配布先ディレクトリ名として使い、`japanese-tech-writing` を alias 付き外部依存として入れ直しても 4 配布面すべてに正しい名前で並ぶこと。`scripts/apm-workspace.ps1` も同時に直す。既存の alias 無し依存の配布名を変えないこと +apm @deploy @decision
