2026-07-21 Google Forms 案件専用スキルを optional-skills へ移管した +skill-migration @apm
2026-07-21 Slack アプリ案件専用スキルを optional-skills へ移管した +skill-migration @apm
2026-07-21 premortem を optional-skills へ移管した +skill-migration @apm
2026-07-16 Agentation を caad-loca-bff / ultra-rss-reader の repo-local 管理へ移管した +skill-migration @apm
2026-07-16 UI bundle を ui-ux-pro-max 本体だけの global 配布へ縮小した +skill-migration @apm
2026-08-12 browser automation を複数リポジトリで常時使う global 基盤として維持すると決定した +skill-migration @apm issue:#6
2026-08-21 ca-pass、mdb-api、notica-api、telma-api を global から撤去し、利用リポジトリの個別導入へ切り替えた +skill-migration @apm
2026-08-12 Agentation / browser automation の利用リポジトリ特定を完了した +skill-migration @apm
2026-07-21 flat optional skill を GitHub に公開し、tech-talks を単体 ref へ移して lock / deploy を確定した +skill-migration @apm
2026-08-21 skill-inventory.md の移管候補レビューを完了した(browser 系は global 維持、社内 API 系は撤去、understand 系は観察タスクへ分離) +skill-migration @apm
x 2026-09-05 2026-09-05 agmsg-delegationのdetached Codex workerで不要なglobal MCP・pluginを起動しない：catalog/skills/agmsg-delegation/scripts/run-codex-worker.shとCodex worker起動設定を対象に、implement/review workerをagmsg transportに必要な最小構成へ隔離し、Computer Use・Computer History・1Password・Context7・Voicevoxなど未使用MCPの自動起動とmacOSの他アプリアクセス確認を防ぐ。完了条件：生成されるworker homeのconfig.tomlにallowlist以外のMCP・plugin・notifyが残らないこと（tests/run-codex-worker.bats）と、worker起動時の子プロセス一覧に未使用MCPが現れないことを確認し、agmsgのREADY/DONE/REVIEW lifecycleと関連runtime testが通ることを確認する。unified logは起動イベントを記録せず判定手段にならないため条件から外した +security +agmsg @apm
x 2026-09-06 2026-09-05 catalogへrepo固有の値が入るのを既存checkで検出する：catalog/agentsとcatalog/skillsの本文・references・runtime assetsの追加変更を対象に、実値入り--profile、ARN・account ID、具体的なcluster/service/domain、repo専用path、pnpm/mise script、存在しないagent委譲先を抽出する。曖昧な候補はfile:line付き警告に留め、機械的に確認できる未解決参照とrepo固有の実行依存が確定したものだけ失敗させる。pre-push/CIのcheckとdeploy前checkへ接続し、CIは差分・配布直前はbaseline比較とする。完了条件：本日廃止したagent3件の旧内容を回帰fixtureとして検出でき、汎用AWS skillのダミー例が通ることを確認し、mise run checkに組み込まれている +apm +security @apm
x 2026-09-06 2026-09-05 catalogからproject repoへの移設に有効化前gateを定める：移設先で有効化される前に、認証/profile・環境リソース識別子・実行script・委譲先・参照pathの契約を移設先の正本と全量照合し、本番操作を含む移設は作者と別のReviewerが有効化前に確認する。.claude/agentsのような自動読込先はコピー自体が有効化にあたるため、isolated branch/worktreeの固定候補でレビューする。完了条件：手順がapm-usageまたはcatalog/AGENTS.mdに書かれ、照合結果の記録形式（記述→移設先の正本path/行→一致/修正/未確認）が定義されている +apm @apm
2026-09-07 mise task 契約テストの取得対象を `global` フラグではなく `source` が対象ディレクトリ配下かで判定し、helper 内で `MISE_ENV` と `MISE_CONFIG_FILE` を unset する：`!task.global` は現ホストでは効くが、`MISE_CONFIG_FILE` 側に task が増えた場合や repo に `mise.<env>.toml` が置かれた場合に `global` フラグの付き方へ依存する。完了条件：source ベースの判定になり、偽の global task を置いた negative fixture で固定されている +apm @tests
2026-09-07 タスクとコマンドの接続を検査するテストを、`run` の完全一致による文字列 snapshot から見直す：`tests/apm-workspace.Tests.ps1` の `expectedCommands` と `tests/apm-workspace.sh.bats` の対応箇所は、空白・等価な引用・`./` パス表記の変更で落ちるため、テストガード整理の「表記変更で落ちない」という完了条件を満たしていない。完了条件：対象 4 task の接続が stub / 実行 trace で確認されるか、snapshot を維持する判断とその根拠がテスト名かコメントに記録されている +apm @tests
2026-09-07 `apm.lock.yaml` の意図しない resolved_commit 移動の発生契機を特定した：原因は `apm.yml` の外部依存 45 件すべてが unpinned（`#<sha>` 形式が 0 件、doctor も unpinned=45）で、install がダウンロードし直したパッケージの resolved_commit が upstream の現在値へ更新されるため。`apm_modules/<owner>/<repo>` の mtime が lock の generated_at 直前に集中していることで裏付けた。`AGENTS.md:48` の "Preserves manifest and lock." は不正確だったため "Preserves the manifest. Does not force an upstream refresh, but unpinned deps re-resolve when re-downloaded." へ訂正した。13 パッケージ分の内容は 2026-09-07 にレビュー済みで受け入れ済み（artifact 削除 0 件、パッケージ増減なし、高リスクなし）。unpinned を続ける運用は維持し、pin へ転換する場合は `pin-external` で 45 件まとめて行う別議題とする +apm @lockfile
x 2026-09-06 2026-09-05 caad-astaへ移設した3 agentから変動値の複製を減らす：cluster名・profile・script名などをagent本文に持たず、repo側の運用skillと実行スクリプトを参照する形へ寄せる。移設時のgateだけではrepo-local化後の再腐敗を防げないため。完了条件：本文の運用値がrepo側正本の参照に置き換わり、terraformとの照合が不要な状態になっている @caad-asta
x 2026-09-07 2026-09-07 公開した Artifact でクリップボード画像コピーが動くことを確認した：docs/diagrams/orchestrator-worker.html を Artifact として公開し、エクスポート（PNG/JPEG/WebP/SVG/WebM と share-card）は downloads capability 経由へ差し替えて閲覧者側でも動くようにしたが、Copy diagram / Copy Share Card が使う navigator.clipboard.write + ClipboardItem は未確認。clipboard 専用の capability は存在せず、artifact の iframe に clipboard-write が許可されているかで決まる。完了条件：https://claude.ai/code/artifact/dc228850-6522-4b49-939e-d875319c91d0 を開いて Copy diagram を押し、成否を本項目へ記録する。動かない場合はボタンを隠すか downloads へ寄せるかを決める +artifact @apm。結果：ユーザーが実機で Copy diagram を押して成功を確認（2026-09-07）。artifact の iframe に clipboard-write が許可されており、navigator.clipboard.write + ClipboardItem は動作する。ボタンを隠す・downloads へ寄せるといった対応は不要。なお当方は実機確認しておらず、根拠はユーザーの報告
x 2026-09-07 2026-09-07 caad-asta の変動値複製の追跡を caad-asta 側へ移した：2026-09-06 に done.txt へ「3 agent から変動値の複製を減らす」を完了記録したが、実態は 3 つのうち aws-operations.md だけで、monitoring-alerts.md に 8 箇所、terraform-operations.md に 17 箇所の app_name 焼き込みが残っている（2026-09-07 に main-cc が照合）。app_name は terraform の変数（terraform/environments/*/shared_variables.tf の default "asta"）なので変更時に 25 箇所が腐る。焼き込みは 3 種類に分かれる: (1) terraform に正本があり参照化できるもの（cluster_name / service_name / backend bucket）、(2) CLI 実行例に埋まっており参照化するとコピペ不能になるもの、(3) app_name 無関係で対象外にすべきもの（asta-deployment は agent 名、asta-stg は DNS ホスト名で terraform から導出不可）。作業は caad-asta 側の asta-cc へ引き継ぎ済み（task_id caad-asta-variable-values-0907、判断 Q1-Q3 を送付）。完了条件：caad-asta 側で (1) が参照化され、(2) の方針が本文または注記に記録され、terraform との照合が不要な状態になっている。main-cc 側の作業はこの起票と引き継ぎで完了 @caad-asta @handoff。訂正（2026-09-07）：本文の「asta-cc へ引き継ぎ済み」は事実と異なる。同 task_id の handoff は 04:12:47 に main-cc 自身が取り下げており（「回答不要。作業を止めてください」）、代わりに main-cc が 3 agent の照合・推奨付与・Luna への委譲を引き取る宣言をしたが、その main-cc セッションは実行前に閉じた。焼き込みは現物で残存（monitoring-alerts.md と terraform-operations.md）。取り下げ直前に astra が返した技術的結論（単純な文字列置換では不可、実行前に対象環境の正本から実値を解決する、両環境の outputs.tf:47 の dashboard_url を参照すれば URL 複製を除去できる）も含め、caad-asta の TODO.md へ追記を依頼して移管した。~/.apm 側に残す作業は無い
x 2026-09-08 2026-09-08 `scripts/apm-workspace.sh` が apm.yml の `alias:` を無視して配布名を決めるのを直す：apm 0.29.0 は object 形式の dependency で `alias: <name>` を受け付け、`apm install --target claude` は `.claude/skills/<alias>/` へ配置する（2026-09-08 に scratchpad で実測）。lockfile に `alias` フィールドは無い（lockfile spec / 実 lockfile ともに 0 件）。apm 側の分岐は `apm_cli/install/phases/integrate.py:612` と `download.py:58` で、alias があれば `apm_modules/<alias>/` へ staging する。lockfile の `name:` は SKILL.md frontmatter 由来の SELF-ASSERTED 値で、upstream lockfile-spec:204 が identity keying への使用を禁じているので配布名の根拠にしない（japanese-tech-writing で一致して見えたのは frontmatter の名前がたまたま alias と同じだったため）。しかし `scripts/apm-workspace.sh` には `alias` の出現が 0 件で、`:1852-1875` の解決は lockfile の `repo_url` と `virtual_path` だけを見る。このため `mise run apply` 経由の rollout では alias が効かず、gist 依存（`https://gist.github.com/k16shikano/fd287c3133457c4fd8f5601d34aa817d.git`）を入れたときに 3 配布面すべてへ `fd287c3133457c4fd8f5601d34aa817d` という gist ID 名で配置された（2026-09-08 実測、その場で差し戻し済み）。影響：repo 名が skill 名として不適な upstream（gist、単一 skill を持つ汎用名 repo）を外部依存にできず、catalog へ vendor するしかない。実例が `japanese-tech-writing`（`docs/package-decisions.md` 参照）。実装済み（2026-09-08、commit `9d820b3`）：`manifest_dependency_alias` を追加して apm.yml の object form の `alias:` を lockfile の `repo_url` と照合し、`external_skill_id_from_record` が alias を優先して返す。alias が無ければ従来の導出へフォールバックする（削除行ゼロで既存の配布名は不変）。照合キーは github 完全 URL・非既定ホスト・SCP 形式・gist の 4 形式を正規化する。`external_skill_content_dir` は `apm_modules/<alias>` を独立ループで先に試す（canonical 側と併存するため同一候補リストに混ぜると ambiguous 判定が誤爆する）。`.ps1` も同じ規約、bats は 105 件 pass。lockfile の `deployed_files` は正本ではなく conformance assert に使う（`name:` は SELF-ASSERTED なので不可）。残る完了条件：`japanese-tech-writing` を alias 付き外部依存として実際に入れ直し、`mise run apply` 経由の rollout で 4 配布面すべてに正しい名前で並ぶことを確認すること。`scripts/apm-workspace.ps1` も同時に直す。既存の alias 無し依存の配布名を変えないこと +apm @deploy @decision
x 2026-09-08 2026-09-08 `~/.config/opencode/skills` が古い catalog を配り続ける経路を整理する：この面だけ `apm_modules/jey3dayo/apm-workspace/catalog/**` 経由で配布されており（manifest の `jey3dayo/apm-workspace/catalog#main`）、他の 4 面（`~/.claude` / `~/.agents` / `~/.opencode` / `~/.cursor`）が `scripts/apm-workspace.sh` でローカル作業ツリーを stage するのと供給元が違う。2026-09-08 実測：`apm_modules/jey3dayo/apm-workspace` は `.git` を持たない取得時点のコピーで、内容は `e3bf8a7` より前。`apm install -g` は ref を refresh せず（`apm install --help` の `--dry-run` 記述）、`deploy:fresh` の `repair:local-package-cache` は `apm_modules/catalog` と `apm_modules/manual-skills` のローカル entry しか作り直さない（`scripts/apm-workspace.sh:1182-1187`）ため、`mise run refresh` / `upgrade` を回すまで追随しない。加えて remote ref を見るので未 push の commit は原理的に届かない。壊れてはおらず「最後に fetch した push 済み catalog」を正しく映しているが、他面と食い違う。OpenCode の skill 探索は frontmatter `name` をキーにした後勝ちで、かつ `Effect.forEach(concurrency:"unbounded")` のため（`sst/opencode` `packages/opencode/src/skill/index.ts:125-139, 240-243`）、内容が食い違う間は precedence が不定になる。調査は `tmp/opencode-skill-paths/report.md`。選択肢：(a) 現状維持し、catalog 変更後は push → `mise run refresh` を運用手順に載せる (b) OpenCode は `~/.claude/skills` と `~/.agents/skills` も global scope で読む（同 `:185-203`、テスト `:273-290`）ので `apm.yml` の `targets:` から `opencode` を外して面を減らす。ただし `.config/opencode/commands` の 5 件が配布されなくなる副作用がある (c) `~/.opencode/skills`（`scripts/apm-workspace.sh:732-738`、走査順 7 で最後）も冗長なので併せて外す。完了条件：いずれかを採用し、catalog 変更後に 4 面の内容が一致するか、意図的に面が減っているかのどちらかになっている +apm @deploy @decision 【決着 2026-09-08】選択肢 (c) を採用。OpenCode は `~/.agents/skills` を global scope で読む（`sst/opencode` `packages/opencode/src/skill/index.ts:185-203`、テスト `:273-290`）ため `~/.opencode/skills` は完全な重複だった。`managed_catalog_runtime_targets` の 4 列目に skills 面なしを表すセンチネル `-` を導入し、opencode をそれに切り替えた。4 列目を `.agents` にする案は不採用——`reconcile_skills_root_from_stage:2469-2476` が stage に無いエントリを刈るため、codex と opencode が同じ root を reconcile すると互いの成果を消し合う。センチネル分岐で既存 `~/.opencode/skills`（76 件）を削除してから skip するので移行は deploy で完結し、実際に消えたことと doctor が `skills=n/a` を返すことを確認済み。`~/.opencode/{CLAUDE.md,agents,commands,rules}` は維持。残る 4 面（`~/.claude` / `~/.agents` / `~/.cursor` / `~/.config/opencode`）は catalog 28 skill で不一致 0/112。`~/.config/opencode` が push 済み ref 経由である点は変わらないので、catalog 変更後は push → `mise run refresh` が必要。
x 2026-09-10 2026-09-10 lock の phantom record（実体不在の 11 skill）の削除は対応不要として閉じる：起点は別セッションの「phantom 19 件」報告と `apm audit --ci` の exit 1。初期仮説「10 missing = 台帳のみの 11 skill」は棄却された（2026-09-10 に main-cc が実測）。missing 10 件は `cathrynlavery/diagram-design` の `.claude|.cursor/commands/{doctor,export-diagram,import-drawio,import-mermaid,profile}.md` で、11 skill と無関係。`apm update -g jey3dayo/apm-workspace/catalog --yes` で catalog の pin を `a226fa5` → `6b1ee70` へ進めたが（`35497e1` で commit、artifact 集合 +6/-0、他依存の resolved_commit は不動）、11 件の record は 1 件も消えず、`.config/opencode/skills` の 4264 行も不変。pin 更新は今日の push を台帳へ反映する別の価値として完了。audit の失敗は root 不一致による所見で、`policy/ci_checks.py:187-241` の `deployed-files-present` は `<project_root>/<path>` を直接 `exists()` で見、`commands/audit.py:1321` が `project_root=Path.cwd()` を渡すため、`~/.apm` で走らせると `~/.apm/.claude/commands/*.md`（ABSENT）を問うが user-scope の配布先は `~/.claude/commands/*.md`（present）である。したがって完了理由は「台帳は履歴だから正常」ではない——`core/deployment_state.py` は DeploymentRecord を ownership metadata と定義し、`install/manifest_reconcile.py:185-205` は同 target の退役 entry を drop する契約を持つので、append-only の履歴帳ではない。閉じる根拠は「件数に基づく初期仮説の棄却」「pin 更新は別の価値として完了」「11 record の削除必要性を示す実害が観測されていない」「audit の所見は別 root の問題」の 4 点。将来の実害の確認点は、同 target 再 install での再出現 / 誤削除、別 owner との競合、uninstall 時の残存、fresh 環境での整合チェック。audit の scope 差は既知の制約として `apm-usage` へ記録したが、exit 1 を恒久的に無視する規則にはしない +apm @decision @apm-audit
x 2026-09-10 2026-09-10 `learning-intake-urr-20260910`（ultra-rss-reader からの NOTIFY、5 項目）を査定し、A のみ採用して残りを不採用として閉じる：判断基準はユーザー指定の「全リポジトリへ共通適用できるものだけ」。採用は A（`agmsg-delegation` の `AGMSG_RESOLVE_PROJECT=0` の適用条件を「別 project を明示して登録する場合」まで広げる）で、`resolve-project.sh` の `agmsg_resolve_project` が marker 取得成功時に入力 path を見ずに返すことを main-cc と astra が独立に確認した。`send.sh` は team config の agents キーの存在だけを照合し登録 project を見ないことも astra が全文確認済み。不採用: B-1「別ディレクトリの git は `git -C`（`cd` は target の hook が走りうる）」は**理由が成立しない**——`git -C` も対象 repo で git を動かすので Git hook への到達性は `cd` 形式と同じで、権限確認の文面 1 件から安全境界の違いは導けない（astra が `git-scm.com/docs/git` の `-C` と `githooks` で確認）。B-2「push 前の定型 rebase をやめる」は意図を支持するが branch protection の有無は rebase 必要性を決めず、「conflict / stale CI のときだけ」ではユーザーの明示指示・repo 必須方針・統合前の正当な更新を除外するため、global の Git 規約へは足さない。C「docs サイトは `<url>.md` で生 markdown」は送信元自身が `ax agent-context` 未読を認めており、URL 末尾への機械的な `.md` 追加は 404 や別版に繋がるため global へは入れない。D「squash merge のメッセージ残存」は `squash_merge_commit_message` が最終本文の強制規則ではなく **default 値**であり、「各 commit は規約に従う」は既存 Git コミット規約 / `atomic-commit` の無条件ルールなので追加不要。E（deny ルールが `--force-with-lease` にマッチしない）は permission 設定の話で、観測 1 件から「絶対に match しない」を規範化できないため現状維持。結果として常時ロード文書（`catalog/AGENTS.md`）への追加は 0 行。選定基準は行数ではなく「全タスクが読む必要のある判断か」 +apm @decision @learning-intake
x 2026-09-10 2026-09-10 `pr-insights-labeler-feedback-global-norms`（5 件）を査定し、global へは 0 行追加で閉じる：採用は 1+2 を `catalog/AGENTS.md:7` の置換に統合し、4 を `orchestrator-worker` §3 の既存行へ吸収した。1 は main-cc が一次確認済み——`command -v git-wt` は `/Users/t00114/.mise/shims/git-wt` を返すのに `git wt --help` は `No manual entry for git-wt` で落ちる（組み込みの `git worktree --help` は man があるので成功）。旧文面が「CLI は `--help`」を一次情報に挙げていたため、送信元は「`git wt` は未導入」と誤報告し、同じ文書の「Git Worktree 方針」（`git wt` 優先）からの逸脱まで下していた。ただし reviewer の指摘により「存在確認は `command -v` か実体」と全 CLI へ固定するのは採らない——`command -v` は解決名 / shim の存在であって本体の実行可能性の証明ではなく、git alias や builtin は `command -v git-xxx` では列挙できない。Git 公式は `git --help ...` を `git help` へ転送すると説明しており（https://git-scm.com/docs/git）、`git wt -h` では usage が取得できるので、man 不在とコマンド不在を分ける文面にした。2 は「不存在の断定だけを覆っている」非対称の解消として同じ行へ統合。3（性能測定の前に負荷を確認する）は **global へ入れない**——`sysctl` 2 本を全 OS の DoD へ要求せず、テスト成否の確認とベンチマーク評価を分け、性能調査 branch の reference へ置く。`loadavg / CPU 数 ≈ 10` という比はそれ自体が CPU 使用率 10 倍や遅延原因の証明ではなく、cold cache 数秒と 427ms も同条件と確認しない限り比較できない。置き場の候補だった `diagnosing-bugs` は 2026-09-10 時点で別セッションが追加中のため作業を混ぜず、着地後に再検討する。5（マージ前に未解決レビュースレッド 0 件を確認）は **却下**——`isResolved: false` は未対応の証明ではなく 0 件も対応済みの証明ではない。既存 `gh-address-comments` は actionable / informational / duplicate を分け、意図的に open を残す結果も扱っており、squash だけに限定する理由も無い。**なお「`gh-address-comments` は catalog に無いので編集できず global しか置けない」という main-cc の推論は誤りだった**——`manual-skills/.apm/skills/gh-address-comments/SKILL.md` が実在する（`catalog/` だけを検索した結果からの誤推論。reviewer が指摘） +apm @decision @learning-intake
